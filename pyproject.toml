[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cell_counter"
version = "0.1.0"
description = "Librería para el conteo de células en imágenes microscópicas"
dependencies = [
    "numpy",
    "matplotlib",
    "opencv-python"
]